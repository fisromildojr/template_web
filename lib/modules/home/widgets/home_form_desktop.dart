import 'package:flutter/material.dart';

class HomeFormDesktop extends StatelessWidget {
  const HomeFormDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
